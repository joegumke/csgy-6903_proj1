1. Create the new virtual  environment :
	python -m venv venv
	
2. Activate the environment:
    venv\Scripts\activate.bat

3. Install packages with pip: 
    pip install -r main.resources\requirements.txt
